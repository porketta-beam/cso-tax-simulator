# Supabase 인증 (M1-a)
#
# 두 값 모두 브라우저 번들에 그대로 들어가는 공개 값이다. publishable key 는
# 공개용으로 설계된 키이며, 실제 보호는 RLS 정책이 한다.
# service_role 키는 이 파일에도 .env.local 에도 절대 넣지 않는다.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
