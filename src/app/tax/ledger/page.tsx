import { Card } from "@/components/design-system";

/** T1 장부 — 목록·월 이동·필터는 PR B 에서. 셸(`../layout.tsx`)이 상단 바를 그린다 */
export default function LedgerScreen() {
  return (
    <Card elevation="none">
      <p className="text-caption leading-normal text-fg-secondary">장부 — 준비 중</p>
    </Card>
  );
}
