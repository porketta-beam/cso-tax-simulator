import { Card } from "@/components/design-system";

/** T2 결과 — 기간 선택·계산 결과·엑셀은 PR C 에서. ⚙ 설정은 셸이 그린다 */
export default function ResultScreen() {
  return (
    <Card elevation="none">
      <p className="text-caption leading-normal text-fg-secondary">결과 — 준비 중</p>
    </Card>
  );
}
